# Agent API
Lots of agents offer their services via agent api. In the current it's illegal to offer other agents service. All agents are only allowed to offer their service for Kubent.

# Quickstart
```bash
uvicorn src.agent_api.main:app --port 20261 --reload
```